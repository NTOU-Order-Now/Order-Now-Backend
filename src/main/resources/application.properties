spring.application.name=Order-Now-Backend
server.port=${PORT}
spring.data.mongodb.uri=mongodb+srv://${DB_USER}:${DB_PASSWORD}@ntou-takeout.iumei.mongodb.net/?retryWrites=true&w=majority&appName=NTOU-Takeout
spring.data.mongodb.database=${DB_NAME}
cors.allowedOrigins=${ALLOWED_ORIGIN}
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
